# 모든 Java 파일은 LF로 설정
*.java text eol=lf
# Gradle, Properties 파일은 LF로 설정
*.gradle text eol=lf
*.properties text eol=lf
# 대용량 파일은 Git LFS로 관리
*.psd filter=lfs diff=lfs merge=lfs -text
*.jar filter=lfs diff=lfs merge=lfs -text
